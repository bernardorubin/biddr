<%= simple_form_for @auction do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :endson, as: :text, input_html: { class: 'datepicker'}%>
  <%= f.input :reserveprice, input_html: {:step => ".01"}  %>
  <%= f.button :submit %>
<% end %>
